{
  "name": "broker-registry",
  "version": "1.0.0",
  "description": "Broker Registry Platform with Supabase & Express backend",
  "main": "ledger-client/server/server.js",
  "type": "module",
  "scripts": {
    "dev": "concurrently -n \"server,client\" -c \"blue,green\" \"npm run server\" \"npm run client\"",
    "start": "node ledger-client/server/server.js",
    "server": "node ledger-client/server/server.js",
    "client": "npm --prefix ledger-client run dev",
    "build": "npm --prefix ledger-client run build",
    "postinstall": "npm --prefix ledger-client install"
  },
  "workspaces": [
    "ledger-client"
  ],
  "dependencies": {
    "@supabase/ssr": "^0.12.7",
    "@supabase/supabase-js": "^2.116.0",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "pg": "^8.23.0"
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
